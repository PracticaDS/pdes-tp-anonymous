import './css/Components.css';
import Toolbox from './Toolbox';

export default class Controller extends Toolbox {
  constructor(props, alt, imagen) {
    super(props, 'controller', alt, imagen);
  }
}
